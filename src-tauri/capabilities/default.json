{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The game calls no Tauri APIs; this grants the window nothing beyond the core defaults.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
